# so in yoututbe video
the clean up function is behave in react when we connect the app than we are running the use effect but when we releas from the app the effect still running that cost the memory leak and burden on the server so after we release the app and leave the app the clean up function is clean the memory that we used

